name: <%= @args[0] %>
root: ~/Workspace/<%= @args[0] %>

windows:
  - editor: vim
  - console: rails c
  - logs:
      layout: main-horizontal
      panes:
        - tail -f log/development.log
        - powder log
