name: <%= @args[0] %>
root: ~/Workspace/<%= @args[0] %>

windows:
  - editor: eval $EDITOR
  - console:
  - logs:
      layout: main-horizontal
      panes:
        - tail -f log/development.log
        - powder log
