<% @dir = "#{Dir.home}/Google Drive/OSU/#{@args[0]}/#{@args[1]}" %>
<% if !Dir.exists?(@dir) %>
  <% Dir.mkdir(@dir) %>
<% end %>

name: <%= @args[0] %>
root: <%= @dir %>

windows:
  - editor: vim
  - other:
